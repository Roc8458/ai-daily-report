# ================= AI 日报 SMTP 配置 =================
# 使用方法：复制本文件为 .env，然后填写下面 3 个必填项
#   cp .env.example .env && vim .env
#
# ⚠️ 安全提示：
#   - .env 含你的私密授权码，已被 .gitignore 排除，请勿提交到任何公开仓库
#   - 授权码获取方式见 references/smtp-guide.md（不是邮箱登录密码！）

# ---------- 必填（3 项） ----------

# 【必填】发件邮箱（同时是 SMTP 登录账号）
SMTP_USER=你的邮箱@qq.com

# 【必填】SMTP 授权码（QQ 邮箱：设置→账号→开启SMTP服务→生成授权码）
SMTP_PASS=你的SMTP授权码

# 【必填】收件邮箱（发给自己填同一个；多个收件人用英文逗号分隔）
TO_EMAIL=你的邮箱@qq.com

# ---------- 可选（留空则自动推断） ----------

# SMTP 服务器（不填则按 SMTP_USER 的邮箱域名自动推断，如 qq→smtp.qq.com）
#SMTP_HOST=smtp.qq.com

# SMTP 端口（465=SSL 加密；587=STARTTLS。不填自动推断）
#SMTP_PORT=465

# 加密方式（1=SSL，0=STARTTLS。不填自动推断）
#SMTP_SSL=1

# 发件人显示名
#SENDER_NAME=AI 日报助手

# 邮件主题前缀
#MAIL_SUBJECT_PREFIX=🤖 AI 每日早报
